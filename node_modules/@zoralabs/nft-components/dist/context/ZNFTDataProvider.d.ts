import React from "react";
import { useNFTType, useNFTMetadataType } from "@zoralabs/nft-hooks";
export declare type ZNFTDataProviderProps = {
    id: string;
    children: React.ReactNode;
    refreshInterval?: number;
    initialData?: {
        nft?: useNFTType["data"];
        metadata?: useNFTMetadataType["metadata"];
    };
};
export declare const ZNFTDataProvider: ({ id, children, refreshInterval, initialData, }: ZNFTDataProviderProps) => JSX.Element;
