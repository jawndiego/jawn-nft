"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCurrenciesInUse = void 0;
function getCurrenciesInUse(nftData) {
    var _a, _b, _c, _d, _e;
    const hasActiveReserveAuction = ((_a = nftData.pricing.reserve) === null || _a === void 0 ? void 0 : _a.status) === 'Active';
    if (hasActiveReserveAuction) {
        const auctionCurrencyId = (_b = nftData.pricing.reserve) === null || _b === void 0 ? void 0 : _b.auctionCurrency.id;
        if (auctionCurrencyId) {
            return [auctionCurrencyId];
        }
    }
    if (!hasActiveReserveAuction) {
        const bids = ((_d = (_c = nftData.pricing.perpetual) === null || _c === void 0 ? void 0 : _c.bids) === null || _d === void 0 ? void 0 : _d.map((bid) => bid.pricing.currency.id)) || [];
        const ask = (_e = nftData.pricing.perpetual.ask) === null || _e === void 0 ? void 0 : _e.pricing.currency.id;
        if (ask) {
            return [...bids, ask];
        }
        return bids;
    }
    return [];
}
exports.getCurrenciesInUse = getCurrenciesInUse;
